Ниже дан список терминов, полученный из научной статьи.

Список терминов:
{terms}

Также дан вопрос пользователя:
"{question}"

Определи, какие термины из предоставленного списка упоминаются или подразумеваются в вопросе. 
Учитывай:
- синонимичные формулировки,
- морфологические варианты,
- краткие формы,
- концептуальные совпадения (например, "encoder" → "transformer encoder").

Формат вывода — JSON-массив только с терминами из списка, которые встречаются в вопросе.

Пример:
{{
    "terms": ["attention mechanism", "encoder block"]
}}
